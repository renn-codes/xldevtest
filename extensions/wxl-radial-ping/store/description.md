# Radial Ping

Adds retail-style party and raid pings with native cursor picking, moving-unit tracking, and
world-to-interface projection. A compatible server validates and relays accepted pings to group
members on the same map.
